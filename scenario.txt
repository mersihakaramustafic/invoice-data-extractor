Invoice data extractor Scenario (high level)

- Hundreds of the invoices are processed daily.
- Invoices are processed on schedule (every 1hour).
- Through the UI, invoices are uploaded into Supabase bucket.
- On schedule, invoices are pulled from the bucket, and processed through LLMs.
- Data is being saved to Supabase database.
- Evaluations are tracked in Langfuse.

To do:
organize architecture: how invoices are supposed to be processed: in batches, how many, sequentally or in paralel